# FAISS GPU requirements for CUDA 11.8
# For systems with NVIDIA GPU and CUDA 11.8 support
# Requires: NVIDIA Driver >= R520
# Install with: pip install -r requirements-faiss-gpu-cu11.txt

faiss-gpu-cu11==1.12.0
